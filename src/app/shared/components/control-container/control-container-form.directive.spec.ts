import { ControlContainerFormDirective } from './control-container-form.directive';

describe('ControlContainerFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlContainerFormDirective();
    expect(directive).toBeTruthy();
  });
});
