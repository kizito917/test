import { BreckLinePipe } from './breck-line.pipe';

describe('BreckLinePipe', () => {
  it('create an instance', () => {
    const pipe = new BreckLinePipe();
    expect(pipe).toBeTruthy();
  });
});
